[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "scnehaux-codex-authority"
version = "0.1.0"
description = "Independent governance authority for scnehaux/codex"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
