# PR #29 Slice 11.3 privileged attestation

Recorded: 2026-09-22 (Asia/Jakarta). Implementation-local, non-normative.

This attestation authorizes only Codex PR #29 at exact base
`3ea9cb421dc5d44d57ca5242d06615fb1950758f` and exact head
`da5294a8d90e2a94b68dd897e0729236bfc85ada`, with the complete sorted
14-path candidate manifest.

The candidate cuts relationship semantic authority over to the governed
declarative ontology while preserving existing graph, repository, rendering and
validation behavior. It removes Python-authored RelationshipSpec declarations
and retains only compatibility/behavior facades.

The candidate does not introduce the full ExecutableFramework. Slice 11.4 remains
the deterministic compiler boundary that will subsume the temporary typed artifact
and relationship projections. JSON Schema remains structural/projection-only.

This record does not override qualification, source identity, candidate drift or
any non-privilege failure. Candidate code is not executed by Authority and
evaluation remains credential-free. Privileged bootstrap remains disabled.

After this attestation merges, only a fresh permanent-runtime PASS for this exact
head may produce evidence, receipt and permit. Publication remains a separate,
short-lived, digest-bound capability.
