# PR #32 Slice 11.6 publication activation

Recorded: 2026-09-24 (Asia/Jakarta). Implementation-local, non-normative.

This change enables one short-lived `attested-v2` publication for Codex PR #32 at
exact head `fa733509974b048c587e9066690cfa7267313b77` and base
`bfea97e841616a2880e28d2cfa177712209520f6`. It follows successful exact-head
Governance Qualification, merged attestation PR #51, and a fresh permanent-runtime
PASS with durable evidence, linking receipt and publication permit.

Authority service and exported publisher source are pinned to `49416a36cae7df0671cc307c5faf40ecf2a97f6d`.
No candidate code executes during Authority evaluation; no evaluation credential is
used. Privileged bootstrap and the generic/default publisher stay disabled.

The governed window is `2026-09-23T19:30:12Z` through
`2026-09-23T19:45:12Z`. The activation is bound to the exact candidate,
source blobs, permit digest, evidence digest and receipt digest. The exported
publisher must be byte-verified and receive the merged configuration before any
write. It rechecks candidate identity and qualification immediately before the
single check POST; an attempt journal prevents blind replay and token revocation
is mandatory. Disarm through a protected Authority PR after the attempt and
successful normal Codex merge, or on failure. This change grants no standing
publication capability and makes no new global enforcement/readiness claim.

The current-state test is rebound from the prior Slice 11.5 disabled state to this
exact PR #32 activation, following the existing transition pattern. The separate
disabled-configuration short-circuit test remains unchanged and still proves zero
credential access and zero network activity when disabled. Completion must restore
the checked-in disabled-state test. No production publisher source is changed.
