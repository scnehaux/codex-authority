# Phase 10 governed status closure and publication disarm

Recorded: 2026-09-21 (Asia/Jakarta). Implementation-local completion evidence.

## Completed governed path

The owner-approved REC-D-018 composed-evidence method and all ten scoped acceptance
rows are recorded by Authority PR #31 / note 0019. Codex PR #26 has now merged
normally, without bypass, as `915d24e861ededec0c516e9b9a95b4ca83796651`.
Its final head was `07a393a24c29d19de8da703c49e22a1ce30f6b51`, with base
`107f0dc53e873ef6d24a9f12cd28da7348f79331`. Only PLAN.md and ROADMAP.md changed.

Current clean-checkout candidate qualification passed at check `106142550431`.
The existing promoted runtime generated durable evidence, receipt and permit
from Authority service source `360d9729310bf2903f4456f5b4464aaac17f873a`.
The exact candidate activation was independently reviewed in Authority PR #32
and merged at `144ce1edc92fe39694df49a90989fb93ac81f219`.

The separate publisher exported from that same reviewed source published check
`106143120366`, `Codex Governance Authority`. Provider readback confirmed exact
head, completed/success, App ID `4864946`, slug `scnehaux-codex-authority`, owner
`scnehaux`. The journal reports token revocation. No second POST was attempted.

The first candidate had text-transport replacement characters in new headings.
Final review corrected them BEFORE publication. That changed head, invalidated the
old bundle, and required fresh qualification and a new exact-head bundle. The
superseded `5bcff61...` bundle remains local history, not a reused capability.

## Post-merge observations and disarm

The [completion record](../../governance/evidence/phase10-completion-001.json)
retains selected provider reads, request IDs/timestamps, the publication journal,
permit/evidence/receipt digests, the actual merge, and the post-merge observer.
The observer reports installed / active / aligned at `915d24e...`, with zero drift.
Named and effective rules match the preflight/tested controls. The two semantic
policy source blobs remain unchanged. Readbacks are sequential selected records,
not a provider signature or atomic snapshot. The public observer's original legacy
401 note remains; the separate preflight admin 404 established no legacy layer.

This change returns `governance/controlled-publication.json` to disabled/unbound
and restores the checked-in disabled-config regression test. The disarm takes
effect upon the normal reviewed merge of THIS completion change. Its PR checkpoint
must record successful CI, merge, and clean-main readback; no future merge SHA is
invented inside this artifact. The privileged bootstrap remains disabled and the
permanent reader remains promoted. No key, provider rule or App scope is changed.

## Final status and retained boundaries

Phase 10 is DONE for the declared GitHub reference-provider scope. The new completion
record sets `phase10_status_finalized: true` and retains scoped system-level
`effective_enforcement_proven: true`. The previous acceptance record correctly keeps
its historical pre-status-merge `phase10_status_finalized: false`. Component
contracts and runtime self-reports retain their own false global-proof fields;
no runtime parser or historical evidence was weakened to display a global flag.

The complete mirror remains BLOCKED. Native Git default deletion is not claimed;
REST native default guard and isolated custom deletion remain the accepted evidence.
Independent human review is not proven. The visible zero-mandatory-approval REVIEW
bootstrap is still active; it is distinct from the disabled PRIVILEGED bootstrap.
Policy, provider, App/runtime, access or review-exception changes require reassessment
under the acceptance record's invalidation conditions. This is no standing permit.

The next planned work is Slice 11.1, Declarative Framework Contract. Phase 11 is not
implemented here and Governance 1.0 remains NOT READY. Generated MATURITY.md and the
normative control registry were not edited to fabricate later-phase completion.

## Verification and source records

Ten new offline tests validate the merge/check/candidate links, token revocation,
post-merge observer, disabled configuration, preserved history and limited claims.
They do not execute live provider operations. All unchanged CI gates must pass;
actual counts and merge/disarm confirmation belong in the completion PR checkpoint.
The known Windows checkout-CRLF limitation of the historical proof verifier is not
silenced; its unchanged gate remains required in GitHub CI.

- [Owner method decision](https://github.com/scnehaux/codex-authority/issues/28#issuecomment-5752319468)
- [Scoped acceptance, Authority PR #31](https://github.com/scnehaux/codex-authority/pull/31)
- [Exact publication activation, Authority PR #32](https://github.com/scnehaux/codex-authority/pull/32)
- [Actual Codex status merge, PR #26](https://github.com/scnehaux/codex/pull/26)
