# Phase 10 scoped reference-provider acceptance

Recorded after the owner accepted REC-D-018 on 2026-09-21 (Asia/Jakarta).
Implementation-local acceptance assessment; not a new governance policy.

## Decision and evidence

The owner answered `oke gas` directly to the question approving composed evidence
without widening the production App installation. The assistant recorded that
instruction in [issue #28](https://github.com/scnehaux/codex-authority/issues/28#issuecomment-5752319468).
This is an owner-approved method and assistant assessment, not independent human
review. All ten unchanged Slice 10.10 obligations are assessed in
[phase10-acceptance-001.json](../../governance/evidence/phase10-acceptance-001.json).
Their original evidence and scope limits are carried from the immutable dossier.

The eight source artifacts retain exact baseline blob pins. The previously
Proposed dossier remains historical; it is not rewritten to simulate earlier
approval. No UNKNOWN, tool-blocked or inconsistent result receives proof credit.
The full-mirror installation remains BLOCKED. The owner accepted the explicit
transfer from isolated control behavior plus actual production qualification,
App-bound negative checks and governed positive paths; the ten requirements are
not waived. Native Git default deletion and independent human review are not claimed.

## Fresh preflight

[phase10-final-preflight-001.json](../../governance/evidence/phase10-final-preflight-001.json)
retains selected CLI GET responses with request IDs and timestamps, clean-source
anchors, fixture readbacks and the existing public observer output. The CLI uses
its existing login; credentials were not extracted or given to the evaluator.
Codex baseline is `107f0dc53e873ef6d24a9f12cd28da7348f79331`; Authority baseline
is `251451c98eda2332674384cd56f5f02a0c42e032`.

The active named ruleset 23193929 matches the tested control parameters. The
actual effective branch-rules endpoint returns those same five controls, and the
inherited-inclusive listing contains no additional ruleset. The admin-visible
legacy-protection endpoint returns 404 `Branch not protected`; repository admin
access was independently confirmed. This means no legacy branch-protection layer,
not that main lacks ruleset enforcement. The public observer's 401 note remains
in its original output; the separate admin result resolves that visibility gap.
The observer reports installed / active / aligned with no drift. All three fixture
repositories remain archived with zero open PRs; temporary reviewer access is Read.

These are sequential selected observations, not a provider signature, atomic
snapshot or promise of future state. A broader capture request was tool-blocked;
it supplies no evidence. Narrow selected-field GETs succeeded without changing
safety settings, App access or provider configuration. No destructive test was rerun.

## Claim ownership and evolutionary boundary

`effective_enforcement_proven: true` in the NEW acceptance record means the ten
requirements are accepted for this source-bound GitHub reference-provider scope,
under REC-D-018. It is not a universal or perpetual guarantee. Reassessment is
required on relevant policy, runtime, App, access, review-exception or provider
behavior changes and on contradictory observations.

Component-level fields in maintenance/handover/publication contracts and original
runtime self-reports remain false: those components do not independently certify
system-wide enforcement. Relaxing their parsers merely to display a global true
flag would enlarge the trusted code boundary without improving evidence. The new
record is the system-level assessment; it grants no publication capability.

The zero-mandatory-approval REVIEW bootstrap remains active because no second
independently controlled qualified reviewer is established. The PRIVILEGED
MAINTENANCE bootstrap stays disabled and the permanent reader remains promoted.
This acceptance does not erase the visible temporary review exception or change
its deterministic exit condition.

## Finalization

Acceptance assessment and repository status closure are separate events. This
record sets `phase10_status_finalized: false`. The next Codex PLAN/ROADMAP PR must
pass current qualification, the promoted runtime, durable evidence/receipt/permit,
source-bound App publication and normal merge. A later completion record must
capture that merge, publication disarm and the post-merge state. No historical
flag is retrospectively changed.

The 14 new offline tests validate decision linkage, row completeness, preflight
hashes, effective rules, admin visibility, cleanup and bounded claims. They do not
perform new provider operations. Unchanged Authority CI must pass before merge;
actual test and merge outcomes belong in the PR verification record.

Primary mechanism references: [GitHub rule layering](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/about-rulesets)
and [effective rule API](https://docs.github.com/en/rest/repos/rules#get-rules-for-a-branch).
These describe mechanisms; the owner decision and linked observations determine
this project's scoped acceptance, not platform documentation alone.
